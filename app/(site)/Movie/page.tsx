import { useAPIByType } from "@/utils/api"

const Movie = () => {
  // const { data, error, isLoading } = useAPIByType('movie')

  return (
    <div>
      {/* {data} */}
    </div>
  )
}

export default Movie